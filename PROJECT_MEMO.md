# 项目备忘（Evo-RL 迭代实操建议）

更新时间：2026-05-02

## 当前训练范式结论

- 本项目当前主路线是**离线训练为主**，不是纯在线 RL。
- 推荐采用“**离线打底 -> Human-in-Loop 小步迭代 -> 准在线谨慎放量**”的三阶段推进方式。

## 三阶段迭代策略

### 阶段 1：先用离线把基线跑稳（成功率可复现）

目标：

- 得到可复现的基线策略与评估结果，作为后续迭代比较基准。

执行要点：

- 固定数据集版本、训练配置、随机种子与评估协议。
- 先跑通 `value-train -> value-infer -> policy-train` 闭环，再做参数微调。
- 每次训练都保留 checkpoint、日志与关键指标（成功率、时延、干预比例）。

进入下一阶段条件：

- 连续多次复现实验结果稳定（波动在可接受范围内）。
- 失败样本有明确模式，可通过新增数据覆盖。

---

### 阶段 2：Human-in-Loop 收少量新数据，合并后继续离线训练

目标：

- 低风险引入新分布数据，快速修补策略薄弱场景。

执行要点：

- 使用人机协同采集少量高价值样本（优先覆盖失败/边界场景）。
- 合并数据池后，重复 `value-train -> value-infer -> policy-train`。
- 保持每轮增量可控，避免一次性大规模混入导致分布漂移难定位。

进入下一阶段条件：

- 新增数据能稳定提升目标任务成功率。
- 线上行为明显改善且无新的高风险动作模式。

---

### 阶段 3：小范围“准在线”迭代（短回合、频繁回滚、严格安全阈值）

目标：

- 在真实执行反馈下进行小步快跑优化，同时确保设备与任务安全。

执行要点：

- 仅在受控任务集和短回合内试验，不做大范围放量。
- 设定硬性安全阈值（异常力矩、关节越界、连续失败上限等）。
- 保持高频 checkpoint，出现异常可快速回滚到上一个稳定版本。
- 迭代节奏建议：小改动 -> 小样本验证 -> 通过后再扩大覆盖面。

退出/降级策略：

- 触发安全阈值或成功率持续下降时，立即回滚到最近稳定模型。
- 暂停准在线试验，回到阶段 2 的离线修复路径。

## 实施原则（贯穿全程）

- **可复现优先**：任何改动都要能被配置、数据版本和日志追踪。
- **安全优先**：真机调试先保证安全，再追求速度和上限。
- **小步迭代**：每轮只改变少量变量，保证问题可定位。
- **对照实验**：新策略必须与基线同协议比较，不凭主观体验判断。

